import SwiftUI
import VaneUI

/// Phase 3 shows the design system rather than the product. Phase 4 replaces this with the
/// main screen; until then the catalog is what there is to look at and judge.
struct ContentView: View {
    var body: some View {
        Catalog()
    }
}

#Preview { ContentView() }
